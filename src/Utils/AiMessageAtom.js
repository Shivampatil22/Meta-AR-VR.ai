import { atom, useAtom } from "jotai";
export const item_id = atom(1);

import React from 'react'
export const AiMessage = atom(null);

export const AiMessageAtom = () => {
    return (
        null
    )
}